
# Append this exact, apex-only site block to the existing gateway Caddyfile
# only after reviewing that file and taking a backup. Do not replace it.
wallet.ru.net {
    encode zstd gzip
    reverse_proxy wallet-mono-app:3000
}
