# M2 Fluid Particles — local handoff

Branch: `codex/mono-materials-particles` in `C:/Users/iwwa/.codex/worktrees/aa9e/5-wallet-foundation`, based on packet `e33b3e75d41a30f6c34bd526caf856754afdcae5`. Exact shared ABI `93d6bc26e67f009fa38dfaddd600c4f84d65900f` was cherry-picked as `4e00c59`; it was authored by ORACLE. This implementation owns only `packages/ui/src/background-sandbox/effects/fluid-particles/**`. No registry, host, MonoScene, package or lockfile was authored here.

Import `particleDefinition` from `effects/fluid-particles/index.ts`. ID `fluid-particles`, capability `background`, ABI 2 and effect version 1. `plan` is pure and reports all GPU texture/attachment bytes before `create`; the adapter rechecks the plan, format capability and every FBO. Its output is borrowed opaque display-sRGB. The host owns canvas/context/RAF, visibility/pause/reduced-motion and the 32 MiB total budget with 4 MiB Promo reserve. This adapter has no independent scheduler or event listeners.

Focused pure checks: eight Vitest files cover budget/profile, seeded physical containment, camera projection on portrait/wide, effective sphere geometry, shader syntax conversion, strict schema, pointer bounds and ABI planning/capability gates. `@wallet/ui typecheck`, focused ESLint and `git diff --check` are the static gates. The isolated browser fixture is `probe/index.html`, driven by `probe/run.mjs`; it ran only in ORACLE's allocated GPU slot at `127.0.0.1:3145`. The browser and temporary Vite process were closed afterward, and the port was checked free. Do not restart it outside a new allocated slot.

GPU evidence is in `probe/evidence/results.json`, `particles-portrait.png` and `particles-wide.png`. Environment: Windows (browser UA `Windows NT 10.0`), HeadlessChrome 153.0.8010.12, ANGLE Vulkan SwiftShader (software). Shader compile/link, float/depth FBO completeness, rendering, color change, seeded reset, four viewport resizes, GL error=0, context loss and fresh reload all passed. Active render reported 74 draws (69 PIC/FLIP solver + 5 sphere/AO/shadow/composite/FXAA); paused render reported 5 display draws. The visible blue mass remained inside 320×568, 390×844, 960×640 and 1440×900 scenes; planned storage ranged from 6,630,432 to 16,148,512 bytes. Repeated dispose/reopen returned to the same GPU resource counts: 18 textures, four FBOs, one renderbuffer, 16 effect programs and three effect VAOs. After disposal only the probe compositor's program/shaders/buffer remained.

The first headless benchmark before the lighting adjustment is recorded in `probe/evidence/first-run-benchmark.json`: CPU submission p50/p95 4.4/6.7 ms and frame interval p50/p95 2316.6/2433.2 ms. That run did not capture its renderer name; the follow-up on the same host and launch path reported SwiftShader. These values do not establish hardware/mobile FPS. The calmer lighting was added because the source AO/shadow factors made the dense mass nearly black; source-speed preset still uses the original 0.7/0.8 factors. Owner visual choice, comparison against the live upstream look, integrated host behavior and real device performance remain for the shared review. No product default or released skin is changed.

The existing hash change does not by itself prove spreading. `probe/progression.mjs` is prepared for a **new ORACLE GPU slot** and has not been run. It keeps the source-speed preset's seed, colors, camera and artistic parameters fixed, with the same detail runtime quality as the initial GPU probe. It reads back the actual RGBA32F particle positions at 0, 0.25, 0.5, 1 and at most 2 integrated seconds, compares physical X span and Y mean, writes initial/progressed canvas frames and stops early once measurable spreading appears. Every solver call receives `dt=1/60`; with source `timeScale=1`, integrated time is steps/60 regardless of wall time. In a slow real frame, the adapter's 1/60 cap discards excess time; calm `timeScale=0.08` advances 12.5 times less per identical host step. The script records measured wall time separately. No new renderer or production readback is added; the float readback exists only in the isolated fixture.
