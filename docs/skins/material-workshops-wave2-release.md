# Мастерские материалов — исправления после просмотра и публикация

Дата: 2026-09-25. Основание — прямой запрос владельца в ORCHESTR WALL: исправить замечания, оформить commits/push/PR и разместить на основном хостинге без ожидания нового визуального согласования.

Это разрешение на публикацию общей сборки, не утверждение качества каждого материала и не разрешение переписывать продуктовые пресеты. Новый просмотр состоится уже на сайте. Не закрывать посторонние задачи и не сливать старые PR/V1 автоматически. Если создание PR блокируется доступом, не обходить отказ другим credential и не выдавать его за созданный PR.

## Одна база, один сборщик

- Общий checkpoint: `9ee2e8c40604c359230f6220fed04b005bc8a16c`, ветка `codex/mono-material-workshops`.
- Worktree сборщика: `C:/Users/iwwa/.codex/worktrees/mono-material-workshops/5-wallet-foundation`.
- ORACLE: `01a0cfcc-f35e-7a50-a624-6dda3022b4fb`, единственный редактор shared host/compositor, маршрутов, общей навигации и release/deploy.
- Сохраняется roster M1–M6 из основного packet. Все исполнители и сборщик — GPT-6 Sol / Max. Новых авторов тех же файлов не создавать.
- Авторы работают в своих worktrees; передают только собственные новые commits. Общий preview остаётся на одном origin. Browser/GPU/build — согласованная очередь, без параллельных тяжёлых серверов.

## Что исправляем

| Владелец | Результат |
| --- | --- |
| M1 + host у ORACLE | Pavel Fluid v2 сохраняет понравившийся характер. Проверены tap/splat, pointer cancellation и режим рисования; обычная прокрутка и действия DOM не перехватываются эффектом. |
| M2 | Частицы проверены в общей сцене, не только isolated probe. Исправляется доказанная причина слишком маленькой/неразличимой композиции: camera/target/scale. Реальная физика и bounds остаются. |
| M4 | Для Vault Grid разделены render-дефект и недостаток дизайна. Три профиля и controls имеют видимый смысл; стартовый строгий материал читается как поверхность, не случайные квадраты. Статичность при drift=0 сама по себе не ошибка. |
| M5 + контракт у ORACLE | Pavel Fluid v2 очевиден для нового workspace. Отдельная компактная группа «Края»: затемнение, ширина/мягкость. Defaults off; параметры сохраняются и отменяются вместе с пробой. Blur только при реальной реализации, только в материале, без размытия DOM. Существующие пробы не переписываются. |
| M6 + compositing у ORACLE | Edge-first старт новых проб кнопок, раздельные поверхность/кромка/иконка. Реальные иконки и подписи читаются на светлом материале. Fill остаётся доступным; существующие пробы не мигрируют в новые defaults. |
| ORACLE | Понятный вход Design Lab в мастерские и обратно, безопасная публикация этих маршрутов и один воспроизводимый release. |

Конкретные новые анимации текста и иконок владелец пока не выбрал. Их не изобретать под видом этого исправления; оставить отдельным будущим заданием. M3/Paper не получает дублирующую работу без доказанного дефекта в принадлежащем ему адаптере.

## Навигация

Design Lab — общий вход, а не неочевидный синоним старого Motion Lab. Сверху компактные, подписанные направления: «Фоны», «Кнопки», существующий «Отклик / Motion» и остальные реально реализованные лаборатории. Не добавлять мёртвые пункты «скоро» и новые пустые страницы.

Существующие routes, deep links и сохранённое состояние сохраняются. В каждой мастерской — ясный возврат в Design Lab и переход к другой мастерской. На мобильном — читаемые controls с touch targets, без scale и горизонтального переполнения. Переход с несохранённой пробой использует уже существующее восстановление/предупреждение, не тихо сбрасывает работу.

## Границы публикации и техническая готовность

- Основной хост `wallet.135.181.70.158.nip.io`; существующий MONO `/mono` не заменяется workshop-черновиком. V1 на отдельном `novex.*`, админка и чужие сервисы не меняются.
- Публикуются отдельные редакторские routes. Убрать dev-only gate только у согласованных пользовательских мастерских; не открыть автоматически diagnostics/probes/debug API/непроверенные эксперименты. Не добавлять новое облако.
- Сохранить текущие данные, logo, approved Promo (`ior +1.34`, dispersion 0), продуктовые presets, share/view-сценарии и лицензии источников. Никаких force-push, разрушительных миграций, новых зависимостей или смены домена.
- Общий результат обязан воспроизводиться одним чистым commit: no temporary readback/preserveDrawingBuffer, secrets, test-results или debug-only hooks в release.
- Достаточный финальный gate: typecheck/lint/build; затронутые unit suites; Save/Open/reload, Apply/Cancel/Undo/A-B и независимость двух библиотек; deep links/navigation; 320/390/430/480, клавиатура/touch/scroll; один context/RAF, guards и context failure/recovery. Не повторять одинаковые source-прогоны без новой причины.
- Автоматизированный touch/SwiftShader не является проверкой физического телефона. Эти ограничения можно честно оставить у опубликованной лаборатории, но реальные провалы сохранения, навигации и нечитаемый интерфейс нужно исправить.

После результатов авторов координатор передаёт ORACLE окончательный состав. Сборщик выпускает один artifact по точному SHA, проверяет его на сервере и сообщает публичные ссылки на `/mono`, Design Lab и обе мастерские; GitHub branch/PR; полный SHA, image tag, включения/исключения и точку отката. GitHub commit/PR и localhost не выдавать за состоявшийся deployment.
