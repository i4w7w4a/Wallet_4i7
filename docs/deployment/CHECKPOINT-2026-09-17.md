# Чекпойнт деплоя Wallet_4i7 — 2026-09-17

Работа остановлена по просьбе владельца из-за лимита. **Не считать публичный деплой завершённым.** Пароли в этот файл не заносились.

## Что уже сделано

- Рабочая копия: `C:\Users\iwwa\Documents\1BANK\.worktrees\26-integration-review`, ветка `codex/skin-lab-v2`. Дизайн `/mono` содержит незакоммиченные файлы; обычный clone ветки не равен текущему виду.
- Точный архив исходников с 191 файлом: `C:\Users\iwwa\AppData\Local\Temp\wallet-mono-release-6103fb9d4bb44c6d823f7213d7c39449\wallet-mono-source.tar`, SHA-256 `790bbaa187d3e604cd812a9ecd0d30bf9a3c9551d5ed92524e03075f2ffc435f`; рядом `source-manifest.json` и `SHA256SUMS.txt`. В чистой копии прошли 174 unit-теста, typecheck, lint, production build и 39 `/mono` E2E.
- Linux x64 overlay: в той же временной папке `wallet-mono-linux-x64-overlay.tar.gz`, SHA-256 `6202e03d03bb8bbc08b19a54a5f801eb5f4202e4d352f060a94ca14ea78fb060`. В WSL с Linux Node 24.13.0 после наложения `/` и `/mono` ответили `200`; ссылки зависимостей целы.
- На VPS исходники и overlay проверены по хешам и извлечены в `/opt/wallet-mono/releases/20260916t231643z`. Все 191 исходный файл после наложения совпали с манифестом. `deploy/` и `.dockerignore` переданы отдельным архивом.
- Собран образ `wallet-mono:20260916t231643z`, ID `sha256:4c146cf1ca3d355236e21f0aa72db73e986320b9f194c27552b0437c1fa6b8c5`. Запущен **отдельный** Compose-проект `wallet-mono`; контейнер `wallet-mono-app-1` healthy. Host-порт не опубликован. Из сети `dlc-dm_default` `/` и `/mono` отвечают `200`.
- Действующий сайт на самом деле `dlc.ru.net`, не `dlc.ru.com` со скриншота. Его HTTPS после запуска нового контейнера: `200`, TLS verification `0`, 19451 байт. Админка по прямому `http://127.0.0.1:5174/`: `200`, 459 байт. `admin.dlc.ru.net` существует как маршрут Caddy, но публичный DNS имени — NXDOMAIN. Старый Compose не изменялся.
- Новый локальный runbook: `docs/deployment/README.md`; шаблоны в `deploy/`. Обнаружен legacy Docker builder без Buildx: он игнорирует Dockerfile-specific ignore. Успешная сборка потребовала временно подменить `.dockerignore` **только в новом release-каталоге** на `deploy/Dockerfile.prebuilt.dockerignore`; исходный `.dockerignore` восстановлен и проверен по хешу. Runbook дополнен процедурой с backup/`EXIT` trap **после** передачи первого deploy-архива, поэтому копия README на VPS сейчас устарела; перед следующим релизом синхронизировать.

## Что НЕ сделано

- Живой Caddyfile `/opt/dlc-dm/app/deploy/Caddyfile` **не изменён и не перезагружался**. Его preflight SHA-256 `ad829534255c5a2cb1a06547f3e826cd0385a9e6598ea49335ec9a1f7999869b`, inode `411511`, размер `568` байт. Отдельный `/opt/wallet-mono/releases/20260916t231643z/Caddyfile.candidate` с новым точным site block прошёл `caddy validate`, но **не применялся**. Из-за одиночного bind mount живой Caddyfile в будущем менять только in-place, затем сравнить хеш host/container перед reload.
- DNS `liquid-prince.online` агент **не менял**. На момент проверки apex `A=31.31.196.17`, `AAAA=2a00:f940:2:2:1:1:0:256`, NS `ns1.hosting.reg.ru`/`ns2.hosting.reg.ru`; у VPS нет публичного IPv6. Пользователю объяснены два действия в REG.ru: заменить только apex A на `135.181.70.158` и удалить только этот apex AAAA. `NS`, `TXT`, почту и `www` не трогать. Ответа «готово» ещё не было. **При продолжении проверить DNS заново:** пользователь мог внести изменения во время паузы.
- Публичный `https://liquid-prince.online/mono` ещё не проверен и может не работать. Сертификат для нового имени ещё не получен.

## Продолжение после возобновления

1. Прочитать этот чекпойнт и `docs/deployment/README.md`; не пересобирать уже проверенный исходник без причины, не трогать старый Compose.
2. Read-only проверить контейнер `wallet-mono-app-1`, старый сайт и админку, живой Caddyfile (hash/inode), авторитетные DNS `A/AAAA` нового apex.
3. Если пользователь ещё не поменял DNS — ждать его сообщения; не включать HTTPS-блок Caddy преждевременно, чтобы не провоцировать неудачные ACME-проверки. Если значения DNS отличаются от ожидаемых, выяснить прежде чем менять.
4. Когда apex A указывает на VPS и старой AAAA нет, по runbook сделать backup живого Caddyfile и **добавить в конец in-place** только проверенный `deploy/Caddyfile.wallet.snippet`; сверить host/container хеш, `caddy validate`, `caddy reload`. При любом ухудшении старого сайта — точечный откат только нового блока.
5. Проверить HTTPS `/` и `/mono`, ассеты/шрифты/мобильный вид и baseline `dlc.ru.net` + прямой порт админки. Отчитаться одной ссылкой и коротким итогом. Если DNS-панель недоступна, честно сказать, что внутренний деплой готов, а публичный адрес ждёт DNS.

Владелец явно решил работать с ранее показанными паролями без ротации. Не хранить их в файлах, командах и отчётах; рекомендация сменить их остаётся, но не блокирует задачу.

## Дополнение, отменяющее прежний доменный план — 2026-09-17

Текст выше сохранён как история уже выполненных проверок. Все его указания использовать `liquid-prince.online` отменены этим дополнением; старое имя не применять к DNS, Caddy или проверкам.

- Новый подтверждённый apex — `wallet.ru.net`. По состоянию, переданному владельцем, его авторитетные NS — `ns1.nameself.com` и `ns2.nameself.com`, а записи `A` и `AAAA` отсутствуют. Во время этой правки живые DNS, сервер и панель не проверялись; перед изменением повторить авторитетный read-only preflight и остановиться при расхождении.
- Планируемый DNS diff — создать только apex `A wallet.ru.net = 135.181.70.158`. `AAAA` не создавать. `NS`, другие RRset, `www`, действующий `dlc.ru.net` и его baseline не менять.
- Ранее проверенный `Caddyfile.candidate` и переданная на VPS копия deploy-архива содержат старое имя и **не пригодны для применения**. Сначала передать обновлённый `deploy/Caddyfile.wallet.snippet`, проверить его checksum и убедиться, что он добавляет только точный блок `wallet.ru.net`; затем заново собрать candidate, сверить inode/hash host и контейнера, выполнить `caddy validate` и только после успеха — `caddy reload`.
- Hold point сохраняется: Caddy-блок не добавлять, пока свежий авторитетный `A` не равен `135.181.70.158` и `AAAA` не пуст. После reload проверить `https://wallet.ru.net/`, `https://wallet.ru.net/mono`, затем неизменённые baseline `dlc.ru.net` и прямого порта админки.
- DNS-откат при неизменном исходном baseline: удалить только добавленный `A 135.181.70.158`, оставить `AAAA` отсутствующим. Caddy откатывать только точечным in-place удалением нового wallet-блока по процедуре из runbook; старый Compose, маршруты и сертификаты не трогать.
